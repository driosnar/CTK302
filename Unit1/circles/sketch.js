function setup() {
  createCanvas(400, 400);
}

function draw() {
  //print("gang")
  //background(220);
  fill (random(255,0),random(255,0),random(255,0),random(255,0));
  ellipse(mouseX,mouseY,90,90);
}
